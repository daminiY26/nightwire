// Re-exported for convenience so app code can `import { cn } from "@/lib/utils"`
// without reaching into @nightwire/ui directly. The real implementation lives
// in packages/ui — see that package if you need to change cn() itself.
export { cn } from "@nightwire/ui";
